#include "download_file_from_url.h"
#include <stdbool.h>

int	convert_links(char **link, struct file_data file_data, bool is_mirror)
{
	if (!link)
		return 1;
	(void)link;
	(void)file_data;
	(void)is_mirror;
	return 0;
}
