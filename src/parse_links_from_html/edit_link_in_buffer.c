#include <stddef.h>

/**
 * @param link_quote_pos The pointer to the open quote of the url
 * @return The new line_buffer
 */
void	edit_link_in_buffer(char **lines, char *link_quote_pos, char *new_url)
{
	(void)link_quote_pos;
	(void)new_url;
	(void)lines;
}
