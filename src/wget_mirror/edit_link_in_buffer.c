#include <stddef.h>

/**
 * @param url_pos The pointer to the open quote of the url
 * @return The new line_buffer
 */
char	*edit_link_in_buffer(char *line_buffer, int buffer_max_size,
												char *url_pos, char *new_url)
{
	(void)line_buffer;
	(void)buffer_max_size;
	(void)url_pos;
	(void)new_url;
	return NULL;
}
